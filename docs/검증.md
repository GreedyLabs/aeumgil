# 검증 안내와 릴리스 기록

검사 실행 결과·운영 반영·미검증 범위를 분리한다. 아래 날짜가 있는 릴리스 기록은 해당 코드의 증거이며 이후 변경까지 자동으로 보증하지 않는다.

## 실행 방법

```bash
pnpm --filter @eumgil/web exec next typegen
pnpm typecheck
pnpm --filter @eumgil/web test
pnpm build
pnpm --filter @eumgil/web verify:db
pnpm --filter @eumgil/web verify:api
pnpm --filter @eumgil/web verify:keycloak
```

Next.js 빌드는 실행 중인 개발 서버와 같은 `.next`를 사용한다. 개발 서버를 멈추고 빌드한 뒤 필요한 모드로 다시 실행한다. 환경변수를 바꿨을 때에도 서버를 다시 시작한다.

### 브라우저

```bash
E2E_BASE_URL=http://localhost:3000 E2E_BROWSER_CHANNEL=chrome pnpm --filter @eumgil/web test:e2e
```

`E2E_BASE_URL`을 생략하면 Playwright가 개발 서버를 준비한다. `E2E_BROWSER_CHANNEL=chrome`은 설치된 Chrome을 사용할 때 지정하며 생략하면 Playwright 브라우저를 사용한다. 설정은 [playwright.config.ts](../apps/web/playwright.config.ts)를 따른다.

브라우저 검사는 실제 DB를 사용한다. 시작 시 `e2e-playwright` 사용자의 6개 테이블 행을 정리하고 테스트 중 생성·수정·삭제한다. 운영 사용자와 분리된 검증 DB 사용을 권장하며, 공유 DB에서 실행할 때는 전용 사용자 범위와 종료 후 정리를 확인한다. 같은 테스트 사용자를 쓰는 여러 실행을 병렬로 돌리지 않는다. 실행 중 화면 코드를 편집하면 HMR로 입력이 초기화될 수 있으므로 코드를 고정한 뒤 검사한다.

앱 인증 검사는 [세션 픽스처](../apps/web/e2e/helpers/session.ts)를 사용한다. 이것은 실제 Keycloak/Google 로그인 왕복을 검증한 것이 아니다. 인증 서버 미리보기와 운영 계정 검사 결과를 별도로 남긴다.

### API와 데이터

`verify:api`는 실제 API를 호출하고 키를 숨긴 응답 요약을 출력한다. 테스트 대역의 정규화 검사와 실제 승인·키·응답 검사를 구분한다. `verify:db`는 연결 외에 현재 조회하는 테이블·컬럼을 확인한다. 카탈로그 수집을 실행한 뒤에는 다음을 확인한다.

- 시군·원천 ID·좌표·분류와 사진 연결.
- 빈 추천 템플릿과 존재하지 않는 장소 참조.
- JSONB 배열의 타입과 idempotent 재실행.
- 무장애 목록 연결 수 및 조건 원문 보존.
- 사용자 기록이 수집/복구 과정에서 유지되는지 여부.

## UX 점검 기준

| 흐름             | 확인할 실패 사례                                                       |
| ---------------- | ---------------------------------------------------------------------- |
| 목적 입력 → 추천 | 지역/활동/동행 혼합, 조사·영문·무관한 입력·부정, 결과 설명과 재검색    |
| 탐색             | 0건·다중 키워드·한글 조합 입력·필터 변경·선택 카드와 지도 일치         |
| 일정 편집        | 추가/중복/순서/일차 변경, 다른 탭 저장 충돌, 이동 중 초안 손실         |
| 모바일           | 320/390px, 긴 주소·제목, 지도 열기/닫기, 키보드와 하단 메뉴/저장 동작  |
| PC               | 1280/1440/1920px, 사이드바 접기, 목록·지도·요약의 가용 폭, 문서 스크롤 |
| 실제 정보        | 이미지 로딩·누락, 행사 기간·인근 거리, 무장애 조건, 관측/예측 시각     |
| 인증             | 원래 페이지 복귀·만료·로그아웃·보호 경로·타 사용자 데이터 접근 거부    |

시각·동선 점검은 기능 테스트와 함께 수행한다. 반응형 테스트의 가로 넘침 0은 필요 조건이며 충분 조건이 아니다.

## 직전 운영 릴리스 — 2026-09-05

| 항목           | 확인 결과                                                                                                              |
| -------------- | ---------------------------------------------------------------------------------------------------------------------- |
| 앱             | `8086565ca8de565649a1a4c509003821b7b898fb`                                                                             |
| Gitops         | `3128600`                                                                                                              |
| CI             | [Actions 33961279812](https://github.com/GreedyLabs/eumgil/actions/runs/33961279812) 검사·GHCR 빌드·Dockhand 웹훅 성공 |
| 단위·타입·빌드 | 단위 197개 통과, 실 LLM 평가 1개 미실행, TypeScript·Next.js 프로덕션 빌드 통과                                         |
| 브라우저       | 개발/프로덕션 Chrome 29개 시나리오 확인                                                                                |
| API            | 공공 API 8종 실제 응답 확인                                                                                            |
| 운영           | 실제 이미지 revision 일치·컨테이너 running/healthy·deep health 정상                                                    |

브라우저 범위: 320/390/768/1280/1440/1920px, 개인 코스 CRUD·타 사용자 404·두 탭 버전 충돌·추천 복사·행사 포함·무장애·리뷰/회원 흐름. 운영 Chrome에서는 홈, 사이드바, 모바일 선택 카드/지도, 행사 소개, 개인 코스 로그인 복귀를 확인했다. 320/390px 가로 넘침과 브라우저 오류는 0이었다. 테스트 전용 사용자 기록은 정리했다.

Keycloak 운영 테마 CSS·Google 버튼·파비콘 버전 스크립트, 독립 Keycloak 26.6.3의 PKCE 로그인 → 확인창 없는 로그아웃 → `prompt=none`의 `login_required`를 확인했다. 실제 Google 자격증명을 입력하는 운영 전체 왕복과 SMTP는 이 자동 검사의 범위 밖이다.

카탈로그 기준선: 강원 원본 4,761건, 관광지 1,975(사진 1,744)·음식점 1,551·숙소 954·테마 119, 무장애 연결 661, 빈 템플릿/끊어진 참조 0/0. 수집 기준은 [카탈로그 문서](관광카탈로그-탐색-고도화-2026-09-05.md)를 따른다.

## 실 LLM 평가

운영은 heuristic이다. `eval:real`은 별도 모델을 명시해 실행하는 실험이며 일반 단위 테스트에서 건너뛴다. 과거 실험 결과는 [에이전트 Evaluation 기록](에이전트-Evaluation-리포트.md)에 보존하되 현재 키워드 매칭이나 운영 응답속도의 근거로 사용하지 않는다.

## 리팩토링 검증 — 배포 전 완료

직전 운영 릴리스 이후 문서를 현행 계약으로 통합하고, 검색을 순수 키워드 매칭과 실제 결과 화면으로 전환했다. 기존 개인 코스에 장소 담기, 여행지/음식점/숙소 종류별 검색, 이동수단·하루 출발 시간 저장, 같은 탭 초안 자동 복구·버전이 다른 초안의 새 코스 복구와 PC/모바일 UI를 변경했다. 검색 결과도 공통 내비게이션을 유지하며 내 여행 명칭을 통일했다. 이 항목은 아직 새 운영 배포 완료 기록이 아니다.

- 실제 DB에 개인 코스 `transport`·`start_time` 컬럼 적용을 확인했다. 기존 행의 기본값은 car/09:30이다.
- 단위 250개 통과, 실 LLM 평가 1개 미실행, TypeScript 검사 통과.
- 개발 서버에서 공개/회원 Chrome 25개와 개인 코스 7개, 총 32개 시나리오 통과. 개인 코스는 기존 코스 추가·종류별 검색·수정 버전 충돌·같은 탭 초안 복원·별도 새 코스 복구까지 확인했다.
- Next.js 프로덕션 빌드와 로컬 `next start`의 Chrome 통합 32개 시나리오가 모두 통과했다(46.5초).
- 운영 반영은 아직이며 커밋·푸시·배포 후 실제 컨테이너와 서비스 결과를 확인해 기록한다.

### 독립 UX 검토와 수정

2026-09-05 개발 빌드를 PC 1440×844, 모바일 390×844·320×844에서 독립적으로 검토했다. 여행 계획의 다음 행동이 보이는지, 조건을 놓치지 않는지, 일정 편집이 끊기는지를 기준으로 지적하고 수정본을 다시 확인했다.

| 항목                   | 수정 전                           | 수정 후 확인                                                           |
| ---------------------- | --------------------------------- | ---------------------------------------------------------------------- |
| 모바일 탐색 첫 카드    | 상단 y765px                       | 390px 폭에서 y412px, 첫 화면 안에서 선택 가능                          |
| 모바일 테마의 코스 CTA | y1422px                           | 약 y290px, 핵심 행동을 사진 앞에 배치                                  |
| 모바일 검색 결과       | 추천 제목·CTA가 큰 사진 뒤에 위치 | 390px 폭에서 제목 y505px·CTA y663px·사진 y786px, 입력 수정은 기본 접힘 |
| 320px 출발 시간        | 2열 입력에서 분 표시 잘림         | 1열·입력 폭 246px, 시간 전체 표시                                      |
| 개인 코스 장소 추가    | 종류별 검색 연결 점검 필요        | 일정/장소 추가 탭, 강릉 음식점·숙소의 실제 검색 결과·일정 복귀 확인    |

최종 수정본에서 가로 넘침·브라우저 오류는 0이었고 추가로 사용을 막는 P1/P2를 찾지 못했다. 검토는 읽기 전용 컨텍스트였으므로 코스 저장·기존 코스 추가·초안 복구·버전 충돌의 DB 검증은 E2E에서 따로 확인한다. 이 결과가 모든 사용자의 UX 문제 부재를 보증하는 것은 아니다.

브라우저 회귀 중 hydration 전에 입력/선택한 값이 유실되는 문제를 발견해 공통 준비 상태 가드로 수정했다. 고정된 코드의 개발 서버와 로컬 프로덕션 빌드에서 각각 32개 브라우저 회귀를 통과했다.
