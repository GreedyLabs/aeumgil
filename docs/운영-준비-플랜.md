# 에움길 운영 준비 플랜 (Production Readiness Plan)

> **이 문서의 목적**: 공모전 제출 + 실제 운영을 위해 남은 작업을 추적한다.
> 어떤 AI Agent/개발자가 와도 이 문서만 보고 작업을 이어갈 수 있도록,
> 각 항목에 **문제 → 근거(파일:라인) → 할 일 → 완료 기준(DoD) → 검증 방법**을 적는다.
>
> **사용 규칙**
> - 작업 완료 시 체크박스를 채우고 날짜를 남긴다. (예: `- [x] ... ✅ 2026-07-15`)
> - 항목을 진행하다 새 이슈를 발견하면 해당 섹션에 같은 형식으로 추가한다.
> - 우선순위: **P0** = 제출·운영에 필수(미해결 시 사고/불능) / **P1** = 운영 품질(첫 주 내) / **P2** = 개선(여유 시).
> - 마일스톤 완료 시 [CLAUDE.md](../CLAUDE.md) "진행 상태" 섹션도 함께 갱신한다.

작성: 2026-07-10 (코드 실사 기반 — dev 브랜치, 커밋 9ba43fe 기준)

---

## 0. 현재 상태 스냅샷 (2026-07-10 실사)

새로 작업을 시작하는 에이전트는 아래를 사실로 전제할 것 (2026-07-10부터 CLAUDE.md는 불변 규칙·아키텍처만 담고, **현황·남은 작업의 정본은 이 문서**다):

| 영역 | 상태 |
|---|---|
| 인증 | **Keycloak(OIDC) 전환 완료.** Auth.js는 Keycloak provider 단일 구성, 소셜(카카오/네이버/구글)은 Keycloak이 브로커링. 앱 DB에 adapter 테이블 없음, `sub`만 사용 ([server/auth.ts](../apps/web/src/server/auth.ts)) |
| 클라이언트 세션 | app-shell이 `useSession`/`signIn`/`signOut` 실배선 완료, mock 인증 제거됨 ([components/app-shell.tsx](../apps/web/src/components/app-shell.tsx)) |
| 쓰기 경로 | Server Actions 4종 완료(saved/profile/onboarding/reviews) — zod 검증 + 세션 가드 + revalidatePath ([app/actions/](../apps/web/src/app/actions/)) |
| 서버 가드 | `requireUserSession` 존재 ([server/require-session.ts](../apps/web/src/server/require-session.ts)) |
| 코스 생성 | `composeCourse`(결정형 조합) + 에이전트 재정렬(`planItinerary`) + 이동시간 포트(approx/Kakao Mobility) 완료. **Kakao 키는 미발급**(`ROUTING_PROVIDER=approx` 동작) |
| LLM | RealLlmProvider(OpenAI-compatible) 구현 완료. `EUMGIL_AGENT_LLM=openai` + `OPENAI_API_KEY`로 활성화 ([server/agent/llm.ts](../apps/web/src/server/agent/llm.ts):396) |
| 인프라 | Dockerfile(standalone, non-root)·GitHub Actions(main/dev push → 이미지 빌드 → Dockhand webhook 배포)·Keycloak compose([infra/keycloak](../infra/keycloak)) 존재 |
| 테스트 | 유닛/정규화 68개 (2026-06-24 로컬 green). E2E·RTL 미착수 |

---

## 1. P0 — 보안·인증 하드닝 (운영 사고 직결)

### 1.1 프로덕션에서 개발용 시크릿 폴백 차단
- **문제**: env 미설정 시 알려진 기본값으로 **조용히** 폴백한다. 운영에서 env가 빠지면 세션 위조가 가능한 고정 시크릿으로 서비스가 뜬다.
- **근거**: [server/auth.ts](../apps/web/src/server/auth.ts):19-21, :100
  - `AUTH_SECRET || "eumgil-dev-only-auth-secret-change-before-production"`
  - `AUTH_KEYCLOAK_SECRET || "eumgil-local-dev-secret"`, issuer 기본 `http://localhost:8080/...`
- **할 일**:
  - [x] `NODE_ENV === "production"`일 때 `AUTH_SECRET`/`AUTH_KEYCLOAK_ID`/`AUTH_KEYCLOAK_SECRET`/`AUTH_KEYCLOAK_ISSUER` 미설정이면 **기동 실패**(throw)하도록 변경. `requireEnv()` 재사용. ✅ 2026-07-10
  - [x] `trustHost: true`(auth.ts:104) 대신 운영에선 `AUTH_URL` 고정 설정을 요구(체크에 포함). ✅ 2026-07-10 — `trustHost: !isProductionRuntime` + `AUTH_URL`을 운영 필수 키에 포함.
- **구현 메모 (2026-07-10)**:
  - `next build`도 `NODE_ENV=production`으로 돌지만 Dockerfile builder 단계에는 시크릿이 없으므로, 단순 NODE_ENV 체크는 Docker 빌드를 깨뜨린다. → `isProductionRuntime`(= production **이면서** `NEXT_PHASE !== "phase-production-build"`)으로 실서비스 프로세스만 판별 ([lib/env.ts](../apps/web/src/lib/env.ts)의 `assertProductionEnv()`).
  - 부팅 시점 검증은 [instrumentation.ts](../apps/web/src/instrumentation.ts)(`register()`, 서버 기동 시 1회), 모듈 로드 시점 이중 방어선은 [server/auth.ts](../apps/web/src/server/auth.ts) 상단에서 동일 함수 호출. 폴백 값은 `authEnv()` 헬퍼로 비운영에서만 적용.
  - 가드 회귀 테스트 4케이스: [lib/env.test.ts](../apps/web/src/lib/env.test.ts) (누락 throw·전체 설정 통과·빌드 페이즈 스킵·dev 통과).
- **DoD**: 운영 모드에서 env 하나라도 빠지면 부팅 시 명확한 에러로 즉사. 로컬 dev는 기존 편의 유지.
- **검증**: `NODE_ENV=production node`로 env 빼고 기동 → 에러 확인. env 채우고 `pnpm --filter @eumgil/web verify:keycloak`.
  - 2026-07-10 샌드박스: typecheck·유닛 111개 green, 가드 로직은 env.test.ts로 검증. **standalone 실기동 검증은 미완** — 샌드박스에서 DB egress 불가로 `next build`가 /discover 프리렌더에서 실패(가드와 무관). 로컬에서 `pnpm --filter @eumgil/web build` 후 `NODE_ENV=production node apps/web/.next/standalone/apps/web/server.js`를 env 없이 실행해 부팅 에러 확인 필요.

### 1.2 Keycloak 운영 인스턴스 점검
- **문제**: 로컬 compose 기준으로만 검증됨. 운영 Keycloak의 HTTPS·realm 설정·소셜 브로커(카카오/네이버/구글) 콜백 URL이 운영 도메인 기준으로 등록돼야 한다.
- **할 일**:
  - [ ] 운영 Keycloak 기동(HTTPS, 관리자 계정 별도 시크릿).
  - [ ] realm `eumgil` 운영 설정: 토큰 수명(현 앱 세션 8h와 정합), 브루트포스 보호 on.
  - [ ] 소셜 IdP 브로커 3종의 redirect URI를 운영 도메인으로 등록 (각 개발자센터 + Keycloak 양쪽).
  - [ ] 운영 client `eumgil-web`의 Valid Redirect URIs를 운영 도메인으로 한정(와일드카드 금지).
- **DoD**: 운영 도메인에서 카카오/네이버/구글 로그인 → 프로필 화면까지 왕복 성공, 로그아웃 시 Keycloak 세션도 종료([api/auth/keycloak/logout](../apps/web/src/app/api/auth/keycloak/logout/route.ts) 경유).
- **검증**: 운영 도메인에서 3개 IdP 각각 수동 로그인/로그아웃. `session.authError` 미발생 확인(토큰 리프레시 경로 auth.ts:48).

### 1.3 시크릿 관리 점검
- **할 일**:
  - [x] `.env`가 커밋되지 않는지 재확인(현재 루트에 `.env` 존재 — `.gitignore` 포함 여부 확인). ✅ 2026-07-10 — `.gitignore:17`에 포함, `git ls-files` 상 추적 파일은 `.env.example`뿐.
  - [ ] 배포 환경(Dockhand) 주입 env 목록을 [.env.example](../.env.example)과 대조해 문서화. 운영 필수 키 목록을 이 문서 부록 A에 유지.
  - [ ] 공공 API 키·OpenAI 키가 클라이언트 번들에 새지 않는지 확인: `grep -r "SERVICE_KEY\|OPENAI" apps/web/.next/static` (빌드 후) 무결과.
- **DoD**: 저장소에 시크릿 0건, 클라이언트 번들에 키 0건.

---

## 2. P0 — 복원력·에러 처리 (첫 장애가 500 화면이 되지 않게)

### 2.1 에러 바운더리 부재
- **문제**: `error.tsx`/`not-found.tsx`/`loading.tsx`가 **한 개도 없다**(`find apps/web/src/app -name "error.tsx"` 무결과). `requireDb()` throw, 공공 API 연쇄 실패, 에이전트 예외 등 어떤 서버 오류든 Next 기본 500 화면이 사용자에게 그대로 노출된다.
- **할 일**:
  - [x] 루트 `app/error.tsx`(클라이언트 컴포넌트, 재시도 버튼) + `app/global-error.tsx` + `app/not-found.tsx` 추가. 톤은 서비스 문구(한국어)로. ✅ 2026-07-10
  - [x] 데이터 무거운 라우트(`course/`, `result/`, `discover/`, `spot/`)에 `loading.tsx` 추가 — 콜드 캐시 시 공공 API 대기(최대 8초 타임아웃×여러 건)가 빈 화면으로 보이는 문제 완화. ✅ 2026-07-10
- **구현 메모 (2026-07-10)**:
  - 공용 폴백 UI는 [components/route-fallback.tsx](../apps/web/src/components/route-fallback.tsx)(`RouteLoading`/`RouteNotice`) — 화면 뷰(screens/*)와 달리 design 프리미티브 없이 전역 토큰만 사용. `error.tsx`는 `reset()` 재시도 + `error.digest` 노출, `global-error.tsx`는 레이아웃 대체 경로라 인라인 스타일만 사용.
  - 검증(2026-07-10, 샌드박스 dev 서버 + DB 불달 상태): 404는 curl로 서비스 문구 렌더 확인. `/discover`는 초기 HTML에 로딩 폴백 SSR + 스트림 페이로드에 에러 digest 확인 — `error.tsx` 자체는 클라이언트 렌더라 curl로는 안 보이며, **브라우저 육안 확인 및 Playwright 시나리오(§8)로 마저 검증할 것**.
- **DoD**: DB를 끄고 각 라우트 접속 → 브랜딩된 에러 화면 + 재시도. 콜드 캐시에서 스피너/스켈레톤 노출.
- **검증**: `DATABASE_URL` 제거 후 dev 서버로 전 라우트 순회. Playwright 시나리오에 포함(§8).

### 2.2 DB 다운 시 열람 경로 완전 불능
- **문제**: 카탈로그(테마/코스/스팟)가 전부 DB 조회라 DB 장애 = 전 화면 불능. 외부 API 실패는 "DB 기본값 유지"로 방어했지만([live/repository.ts](../apps/web/src/data/live/repository.ts):236) DB 자체 실패는 방어가 없다.
- **할 일**:
  - [x] (택1 결정) **② 채택** — 에러 바운더리(§2.1) + "잠시 후 다시" 안내로 수용. ✅ 2026-07-10 — 근거: 공모전 규모에서 DB 상시 장애 대비 스냅샷 서빙(①)은 복잡도 대비 이득이 작고, §2.1 에러 화면 + 재시도 버튼으로 사용자 경험은 확보됨. 카탈로그 캐시 서빙이 필요해지면 P2 "다중 인스턴스 대비"와 함께 재검토.
  - [x] postgres-js 연결 옵션 점검: 연결 타임아웃/재시도, `max` 커넥션 수를 컨테이너 환경에 맞게 명시([db/index.ts](../apps/web/src/server/db/index.ts)). ✅ 2026-07-10 — `connect_timeout: 5`(기본 30s는 장애 시 에러 화면 노출까지 과도) · `max: 10` · `idle_timeout: 60`(순단 후 죽은 커넥션 정리→복귀 추종).
- **DoD**: DB 30초 순단 시나리오에서 서비스가 (스냅샷 서빙 또는 안내 화면으로) 복구되고, 재기동 없이 DB 복귀를 따라온다.
- **검증**: 로컬에서 Postgres stop→start 하며 라우트 순회. (2026-07-10 코드 완료, **로컬 순단 리허설은 미수행** — §8.2 배포 전 체크와 묶어 1회 수행할 것)

### 2.3 콜드 캐시 팬아웃 지연
- **문제**: `listSpots()`는 스팟 전건을 병렬 enrich → 스팟당 날씨+대기질(+관광상세) 호출([live/repository.ts](../apps/web/src/data/live/repository.ts):265). 콜드 캐시에서 discover/map 첫 요청이 수 초 걸릴 수 있고, 요청이 몰리면 공공 API 타임아웃(8s)까지 대기.
- **할 일**:
  - [x] 목록 화면(discover/map/home)은 `enrich: false`로 받고, 상세(spot)만 enrich하는지 라우트별 사용 현황 점검·정리. ✅ 2026-07-10 — 점검 결과: home/saved/map은 이미 `enrich:false`, discover는 스팟 미조회(listThemes만). **reviews/profile을 `enrich:false`로 변경**(화면이 실시간 필드를 안 씀 — reviews는 `visit.congestionThen` 사용자 기록값만 사용) + 순차 await 루프를 병렬화. **enrich 유지가 맞는 곳**: spot 상세·course(타임라인 혼잡 배지)·alternatives(혼잡 비교가 목적) — 셋 다 소수 스팟만 조회.
  - [ ] 배포 직후 워밍: 시작 시(또는 배포 후크로) 주요 키(`wx:*`, `air:*`) 1회 프리페치하는 경량 스크립트 추가(선택).
- **DoD**: 콜드 스타트 후 discover 첫 로드 p95 < 3s(근사 이동시간·캐시 미스 포함).
- **검증**: `EUMGIL_DEBUG=http,cache`로 콜드 기동 후 각 라우트 1회 순회, 로그의 실호출 횟수·ms 기록. (실측은 로컬 — 샌드박스는 공공 API egress 불가)

---

## 3. P0 — 비용·어뷰즈 방어 (공개 서비스의 필수 방어선)

### 3.1 `/result?q=` 무인증·무제한 → LLM/공공 API 소진 벡터
- **문제**: [result/page.tsx](../apps/web/src/app/result/page.tsx)는 임의 쿼리로 `matchThemes()`를 호출하고, 이는 에이전트 루프(+`EUMGIL_AGENT_LLM=openai`면 실 LLM 과금) + 공공 API 호출로 이어진다. 봇이 무작위 q로 두드리면 OpenAI 비용·공공 API 일일 쿼터가 소진된다.
- **할 일**:
  - [x] 입력 방어: `q` 길이 상한(예: 200자)·공백 정규화 후 매칭 진입. 빈/초과 입력은 매칭 없이 안내. ✅ 2026-07-10 — `normalizeIntentQuery`/`INTENT_QUERY_MAX_LENGTH`([domain/matching.ts](../apps/web/src/domain/matching.ts)), 화면 안내는 [result/page.tsx](../apps/web/src/app/result/page.tsx)(RouteNotice), 리포지토리는 방어 절단.
  - [x] 레이트리밋: IP 기준 분당 N회(예: 10회) 초과 시 결정형 폴백만 사용(LLM 미호출). 단일 인스턴스이므로 인메모리 토큰버킷으로 충분 — 미들웨어 또는 matchThemes 진입부에 구현. ✅ 2026-07-10 — [server/rate-limit.ts](../apps/web/src/server/rate-limit.ts) 토큰버킷(용량 10, 분당 10 재충전, IP는 x-forwarded-for), `matchThemes` 진입부에서 캐시 미스 시에만 토큰 소모.
  - [x] LLM 호출 결과 캐시: 동일 정규화 쿼리는 `apiCache`로 단기(예: 1h) 캐시. ✅ 2026-07-10 — 키 `match:{provider}:{정규화쿼리}`, TTL 1h. 트레이드오프: 폴백 결과도 1h 캐시됨(어뷰즈 방어 관점 의도된 동작, 코드 주석에 기록).
  - [ ] OpenAI 대시보드에 월 하드 한도(예산) 설정 — 코드 밖 안전장치. (실키 발급 시 수동 수행)
- **DoD**: 부하 스크립트로 분당 100회 요청 시에도 LLM 호출은 상한 이내, 서비스 응답은 계속 200(폴백).
- **검증**: 간단한 `for` 루프 curl + `EUMGIL_DEBUG` 로그로 LLM/공공 API 호출 수 확인.
  - 2026-07-10: 배선 테스트 [match-guard.test.ts](../apps/web/src/data/live/match-guard.test.ts)로 검증 — 동시 25건 요청 시 에이전트 진입 ≤ 버킷 용량(10), 초과분 포함 전 응답이 유효 ThemeMatch, 동일 쿼리 재요청은 에이전트 미진입(캐시). 토큰버킷 단위 테스트 4종 별도([rate-limit.test.ts](../apps/web/src/server/rate-limit.test.ts)). 실서버 curl 부하는 로컬(DB 연결 가능 환경)에서 1회 확인 권장.

### 3.2 공공데이터포털 쿼터 운영 전환
- **문제**: 개발계정 활용신청은 일일 트래픽 제한(통상 1,000회/일 수준, API마다 상이). 실서비스 트래픽 + 콜드 캐시 팬아웃이 겹치면 한도 초과로 하루 종일 폴백 상태가 될 수 있다.
- **할 일**:
  - [ ] TourAPI/KMA/에어코리아 각각의 현재 일일 한도 확인·기록(포털 마이페이지).
  - [ ] 부족하면 **운영계정 전환(활용사례 등록)** 신청 — 승인 리드타임이 있으므로 제출 2주 전 착수.
  - [ ] 캐시 TTL 재점검: 날씨/대기질 10분([live/repository.ts](../apps/web/src/data/live/repository.ts):74)은 스팟 수 × 144회/일. 스팟 수가 늘 계획이면 TTL 상향 또는 권역 단위 캐시로 통합(날씨는 이미 좌표별, 대기질은 권역별 — 날씨도 권역 단위로 낮출지 결정).
- **DoD**: 예상 일일 호출량(스팟 수 × 24h/TTL × API 종수) < 한도의 50%.
- **검증**: 계산식을 이 문서에 기록 + `EUMGIL_DEBUG=http` 하루 로그로 실측.

### 3.3 Kakao Mobility 이동시간 — 키 발급 및 호출량 통제
- **문제**: 현재 `approx`(근사)로 동작. 키 연동 시 `buildTravelTimeLookup`이 코스 후보 좌표 **N×N 전쌍**을 호출한다(상한 80쌍, [server/routing/travel-time.ts](../apps/web/src/server/routing/travel-time.ts):24). 콜드 캐시에서 코스 1회 조회가 최대 80콜. 또한 `attemptedApiPairs` 증가가 `Promise.all` 병렬 내에서 일어나 상한이 정확히 걸리지 않는다(경쟁 조건 — 상한 초과 발사 가능).
- **할 일**:
  - [ ] Kakao developers 앱 생성·Mobility Directions 키 발급, `.env`에 `KAKAO_MOBILITY_API_KEY` + `ROUTING_PROVIDER=kakao`. (수동 — 키 발급 후 아래 코드 경로는 그대로 동작)
  - [x] 호출쌍 축소: 전쌍(N×N)이 아니라 **코스 타임라인상 인접 구간 + 대체지↔인접 스팟**만 실호출하도록 후보 선정 로직 개선(나머지는 근사 유지). ✅ 2026-07-10 — `BuildTravelTimeLookupOptions.apiPairs` 옵션 추가([server/routing/travel-time.ts](../apps/web/src/server/routing/travel-time.ts)), 호출부는 `timelineApiPairs()`로 composeCourse 의 실제 조회 패턴((prev,후보)·(후보,next))만 생성([data/live/repository.ts](../apps/web/src/data/live/repository.ts)). 템플릿 없는 스크래치 조합은 근사만 사용. 근사 포트일 땐 사전 매트릭스 자체를 생략(조회 시 동일 계산).
  - [x] `attemptedApiPairs` 상한을 사전 슬라이스 방식(호출 목록을 먼저 만들고 상위 M개만 API)으로 바꿔 경쟁 조건 제거. ✅ 2026-07-10 — 카운터 제거, 후보 목록에서 중복·자기쌍 걸러 상한까지만 슬라이스. 주입 포트에도 상한이 균일 적용되도록 단순화. 테스트 2종 추가([travel-time.test.ts](../apps/web/src/server/routing/travel-time.test.ts) — 상한 정확성·apiPairs 선별 호출).
  - [ ] 쿼터 확인(무료 한도) 후 상한 기본값 조정. (수동 — 키 발급과 함께)
- **DoD**: 코스 1회 콜드 조회의 실 API 콜 ≤ 인접 구간 수 + α, 캐시 웜 상태에선 0콜.
- **검증**: `EUMGIL_DEBUG=routing`으로 콜드/웜 각 1회 조회 로그 카운트. 기존 [travel-time.test.ts](../apps/web/src/server/routing/travel-time.test.ts)에 상한 케이스 추가.

---

## 4. P1 — 데이터 완결성 (심사에서 바로 보이는 구멍)

### 4.1 식사(Eat)/숙박(Stay) 데이터 부재
- **문제**: `getEat`/`getStay`/`listEats`/`listStays`가 전부 `null`/`[]`([live/repository.ts](../apps/web/src/data/live/repository.ts):577-591). `composeCourse`는 eats/stays를 빈 배열로 받아([live/repository.ts](../apps/web/src/data/live/repository.ts):352-353) 시드 템플릿의 eat/stay 항목을 대체하지 못하고, 코스 화면의 eat/stay 타임라인 항목([screens/course.tsx](../apps/web/src/components/screens/course.tsx):379-403)은 refId 조회가 빈 맵이라 표시가 비거나 누락된다.
- **할 일**:
  - [x] (택1 결정) **① 채택** — TourAPI `contentTypeId=39`(음식점)/`32`(숙박)로 권역별 후보를 수집해 별도 테이블에 시드 → `listEats`/`listStays` 구현. ✅ 2026-07-10 (사용자 결정 — 추후 별도 배치 프로세스에서 주기 수집으로 이관 가능해야 함)
  - [x] 구현 완료 ✅ 2026-07-10:
    - 테이블 `commerce_profile`(kind=eat|stay, [db/schema.ts](../apps/web/src/server/db/schema.ts)) + `pnpm db:apply` DDL 반영([apply-app-schema.mjs](../apps/web/scripts/apply-app-schema.mjs)).
    - 수집: [scripts/collect-commerce.mjs](../apps/web/scripts/collect-commerce.mjs) (`pnpm db:collect:commerce`, `COLLECT_ROWS`로 건수 조정) — **멱등 upsert라 배치(cron) 이관 시 그대로 주기 실행 가능**. 시군구 코드는 하드코딩 대신 `areaCode2` 런타임 조회로 권역명(REGIONS 8개) 접두 매칭. `region_ko`를 스팟과 동일한 권역명("강릉" 등)으로 저장해 `pickByRegion` 문자열 매칭이 성립.
    - 읽기: [server/db/commerce.ts](../apps/web/src/server/db/commerce.ts) → `LiveRepository` 4메서드 구현, `composeSeedCourse`가 eats/stays 실데이터를 composeCourse에 전달(수집 전/조회 실패 시 빈 후보로 코스 생성은 계속 — warn 로그).
    - 화면: 코스 타임라인 eat/stay 메타에서 빈 가격·평점 0 생략([screens/course.tsx](../apps/web/src/components/screens/course.tsx)). TourAPI에 가격·평점이 없기 때문.
  - [x] `pickByRegion` 경로 테스트 — course-compose 기존 테스트가 커버, 정규화 테스트 추가([server/db/commerce.test.ts](../apps/web/src/server/db/commerce.test.ts)). ✅ 2026-07-10
  - [ ] **로컬 실수집 1회**: `pnpm db:apply` → `pnpm db:collect:commerce` 실행 후 코스 화면에서 eat/stay 항목 표시 확인. (샌드박스는 data.go.kr·DB egress 불가라 미실행)
- **DoD**: 코스 화면에서 빈 항목/이름 없는 항목이 0건.
- **검증**: 전 테마 코스 화면 수동 순회 + course-compose 테스트.

### 4.2 지도 화면 — 하드코딩 좌표·혼잡도
- **문제**: [screens/map.tsx](../apps/web/src/components/screens/map.tsx):13-23 의 POI 목록이 화면 퍼센트 좌표(x/y)·혼잡 레벨(`lvl`)까지 **하드코딩**. 실 혼잡 산출(Phase 3)이 지도에 반영되지 않아 "실연동" 주장과 어긋난다. 스팟이 추가돼도 지도에 안 나온다.
- **할 일**:
  - [ ] 서버 page에서 `listSpots()`(enrich 여부는 §2.3과 함께 결정)로 실 혼잡도·좌표를 내려주고, lat/lon → 화면 좌표 변환(강원 바운딩박스 선형 사상)으로 마커 배치.
  - [ ] `lvl`을 스팟의 실시간 `congestion`으로 대체.
- **DoD**: 지도 마커의 위치·혼잡색이 spot 상세 화면과 동일 데이터에서 나온다.
- **검증**: 특정 스팟의 혼잡 등급을 spot 화면과 지도에서 교차 확인.

### 4.3 저장 화면 잔여 mock
- **문제**: [screens/saved.tsx](../apps/web/src/components/screens/saved.tsx):58-60 — 날짜 `"Apr 24-25"` 하드코딩, 혼잡 `Signal level="calm"` 고정.
- **할 일**:
  - [ ] 날짜: 저장 시각(`saved_theme.created_at`) 또는 표시 제거로 대체.
  - [ ] Signal: 테마 대표 스팟의 실시간 혼잡 또는 표시 제거.
- **DoD**: saved 화면에 하드코딩 문자열 0건.

### 4.4 contentId 미확정 4건 (이미지/개요 보강 공백)
- **문제**: anmok-beach/ojukheon/daegwallyeong-sheep/jumunjin-cafe는 적합 TourAPI POI 부재로 `contentId=null` 확정([data/live/spot-mapping.ts](../apps/web/src/data/live/spot-mapping.ts)) → TourAPI 개요/이미지 보강이 안 된다.
- **할 일**:
  - [ ] 4건의 이미지·설명을 자체 소스(직접 촬영/무료 라이선스, 출처 명시)로 `spot_profile`에 채움 — "홈 히어로만 Unsplash, 나머지는 실 이미지" 방침(커밋 9ba43fe)과 정합.
  - [ ] 분기별 1회 `find:content` 재조회로 신규 POI 등록 여부 확인(TourAPI 데이터는 계속 추가됨).
- **DoD**: 전 스팟 상세 화면에 이미지+설명 표시.

### 4.5 대체지 검색(search_pois) 품질
- **문제**: `searchPois`가 지역기반 목록 30건을 고정 키(`pois:list`)로 캐시한 뒤 제목 근사 필터([live/repository.ts](../apps/web/src/data/live/repository.ts):566-572). 키워드와 무관한 결과가 나오기 쉽고, 강원 전체 3,355건 중 30건만 대상.
- **할 일**:
  - [ ] TourAPI `searchKeyword2`(키워드 검색 오퍼레이션)를 [tourapi.ts](../apps/web/src/server/public-api/tourapi.ts)에 추가하고 `searchPois`가 이를 사용(키워드별 캐시 키).
  - [ ] 대체지 노출 문구(altNote)가 실제 후보 품질과 맞는지 course 화면에서 확인.
- **DoD**: "시장", "해변" 등 키워드로 관련 POI가 상위에 온다.
- **검증**: 유닛 테스트(fetch mock) + 실호출 1회 스크립트.

### 4.6 i18n — 언어 전환 미노출
- **문제**: `TWEAKS.lang = "ko"` 고정([components/app-shell.tsx](../apps/web/src/components/app-shell.tsx):23). 도메인은 전부 `LocalizedText {ko,en}`인데 전환 UI가 없다.
- **할 일**:
  - [x] (결정) **한국어 전용으로 확정** ✅ 2026-07-10 (사용자 결정) — 공모전 범위에서 en 노출하지 않음. 전환 UI·cookie 영속 작업 없음. 도메인 `LocalizedText` 구조와 en 텍스트는 유지(추후 확장 대비, 새 데이터는 en 채움을 강제하지 않음 — `L()` ko 폴백 사용). **이 항목 닫음.**
- **DoD**: 결정 기록 + (노출 시) 전 화면 en 순회 QA. → 결정 기록으로 충족.

---

## 5. P1 — 인프라·배포 운영화

### 5.1 캐시 파일 영속성 — 컨테이너에서 무력화
- **문제**: 캐시 파일 기본 경로가 `os.tmpdir()`([server/cache.ts](../apps/web/src/server/cache.ts):167-169). Docker 재배포/재시작마다 소실돼 "콜드스타트 후에도 캐시 생존" 설계 목적이 운영에서 무력화된다. 재배포 직후가 정확히 트래픽+콜드캐시가 겹치는 시점.
- **할 일**:
  - [ ] 배포 스택(Dockhand)에서 볼륨 마운트 + `EUMGIL_CACHE_DIR=/data/cache` 지정.
  - [ ] 다중 인스턴스 확장 시 파일 캐시 경합 주의를 cache.ts 헤더 주석에 명시(현 단일 인스턴스 전제).
- **DoD**: 재배포 후 첫 요청 로그에 `LOAD n entries from store` 확인.

### 5.2 헬스체크 엔드포인트
- **문제**: `/api`에는 auth 라우트뿐. 배포 시스템·모니터링이 살아있음/의존성 상태를 확인할 방법이 없다.
- **할 일**:
  - [ ] `app/api/health/route.ts` 추가: 기본 200 `{ok:true}` + `?deep=1`이면 DB `select 1`·캐시 로드 여부 포함(공공 API는 포함하지 말 것 — 외부 장애로 헬스가 죽으면 안 됨).
  - [ ] Dockhand/모니터링에서 이 엔드포인트를 주기 체크하도록 설정.
- **DoD**: `curl /api/health` 200, DB 다운 시 `?deep=1`만 503.

### 5.3 관측성 — 에러·외부 API 실패를 볼 수 있게
- **문제**: 로깅이 `EUMGIL_DEBUG` 켰을 때의 콘솔뿐. 운영에서 조용한 폴백(enrich 실패→DB 기본값)이 며칠간 지속돼도 알 수 없다 — "실연동" 서비스가 사실상 정적 서비스로 퇴화해도 모른다.
- **할 일**:
  - [ ] 운영에서 **폴백 발생은 항상 로그**(debug 아닌 warn 레벨, 현 `log.warn`이 EUMGIL_DEBUG 없이도 stderr로 나가는지 [server/log.ts](../apps/web/src/server/log.ts) 확인·조정).
  - [ ] 컨테이너 로그 수집 확인(Dockhand 쪽) + 최소 알림 1개: "공공 API 실패율 급증" 또는 "헬스체크 실패" 시 알림(이미 쓰는 채널로).
  - [ ] (선택, P2) Sentry 등 에러 트래커.
- **DoD**: 공공 API 키를 일부러 틀리게 넣고 배포 환경 로그에서 폴백 로그가 보인다.

### 5.4 배포 파이프라인 마무리
- **할 일**:
  - [ ] dev/main 브랜치 → 배포 환경 매핑 명확화(현재 두 브랜치 모두 push 시 배포 트리거 — staging/production 분리 여부 결정·기록).
  - [ ] 롤백 절차 1회 리허설(이전 이미지 태그로 재배포) 후 절차를 [docs/](.) 런북으로 기록.
  - [ ] DB 마이그레이션 순서 정의: 배포 전 `db:apply`·시드 스크립트 실행 주체(수동/CI) 결정·기록.
- **DoD**: "코드 머지 → 배포 → 문제 시 롤백"을 문서만 보고 수행 가능.

### 5.5 DB 백업
- **할 일**:
  - [ ] `pg_dump` 일 1회 크론 + 보존 7일(사용자 데이터: saved/review/visit/profile — 유일하게 복구 불가능한 데이터).
  - [ ] 복원 리허설 1회.
- **DoD**: 백업 파일에서 로컬 복원 성공 기록.

---

## 6. P1 — 에이전트/LLM 운영 (C·D단계 마무리)

### 6.1 실 LLM 검증 및 안전장치
- **문제**: RealLlmProvider는 구현됐지만 실키 운영 경험이 없다. 사용자 자연어가 그대로 에이전트 intent로 들어가므로([live/repository.ts](../apps/web/src/data/live/repository.ts):486) 프롬프트 인젝션 시도가 가능하다 — 도구 화이트리스트·MAX_STEPS 가드레일은 있으나, **LLM이 생성한 rationale 문자열이 화면에 노출되는 경로**가 있는지 확인 필요.
- **할 일**:
  - [ ] `EUMGIL_AGENT_LLM=openai`로 로컬 실검증: 대표 쿼리 10개(정상 5, 이상 입력 5 — 빈말/영어/장문/인젝션 시도/무관 주제)에서 유효 ThemeMatch + 폴백 동작 확인.
  - [ ] LLM 호출 타임아웃·재시도 정책 확인(llm.ts) — 미설정이면 8s 타임아웃 + 재시도 0회로 명시(사용자 대기 상한).
  - [ ] rationale 등 LLM 산출 텍스트의 화면 노출 지점 조사 → 노출한다면 길이 제한·개행 정리 등 새니타이즈, 노출 안 하면 확인 기록.
  - [ ] §3.1 레이트리밋과 연동(초과 시 heuristic/결정형만).
- **DoD**: 이상 입력 5종에서 화면 오류·이상 문구 노출 0건, 폴백 `source:"fallback"` 로그 확인.

### 6.2 Evaluation을 실모델로 1회전
- **문제**: D단계 eval은 mock provider 위에서만 돌았다. 실모델 품질 근거가 없다.
- **할 일**:
  - [ ] `runEval`을 RealLlmProvider로 실행하는 스크립트(`pnpm --filter @eumgil/web eval:real` 등) 추가 — 러너는 프로바이더 무관이므로 llm만 교체.
  - [ ] mock 대비 실모델 리포트를 [docs/](.)에 저장(공모전 "LLM Evaluation" 자격요건 증빙으로도 사용).
  - [ ] (P2) LLM-as-judge 채점기 추가.
- **DoD**: 실모델 eval 리포트 파일 1개 + 기준선 통과율 기록.

---

## 7. P1 — 법적·정책 요건 (공공데이터 실서비스의 의무)

### 7.1 공공데이터 출처 표기
- **문제**: 공공데이터포털 이용약관·공공누리는 출처 표기를 요구한다. TourAPI 콘텐츠(개요/이미지)·기상청·에어코리아 데이터를 화면에 쓰면서 출처 고지가 없다.
- **할 일**:
  - [ ] 푸터 또는 settings/doc 화면에 데이터 출처 고지: "한국관광공사 TourAPI", "기상청 단기예보", "한국환경공단 에어코리아" + 공공누리 유형.
  - [ ] TourAPI 이미지 사용 조건(저작권 표기) 확인 — `firstimage`는 표기 조건이 있는 경우가 있음. spot 화면 이미지에 출처 캡션 여부 결정.
  - [ ] 에어코리아 고지 문구("실시간 자료로 측정소 사정에 따라…") 검토 — 대기질 등급 노출 화면에 주석.
- **DoD**: 심사자가 어느 화면을 봐도 데이터 출처를 확인 가능.

### 7.2 개인정보처리방침·이용약관
- **문제**: 소셜 로그인으로 이메일·프로필을 수집하는 실서비스는 개인정보처리방침 게시가 법적 의무(개인정보보호법). 카카오/네이버 개발자센터도 앱 심사 시 방침 URL을 요구한다.
- **할 일**:
  - [ ] 개인정보처리방침 페이지(수집 항목: Keycloak 경유 이메일/이름/프로필사진, 보관 기간, 파기 절차) — `doc` 라우트 활용.
  - [ ] 이용약관 페이지(간단 버전).
  - [ ] 회원 탈퇴 경로: Keycloak 계정 삭제 + 앱 DB(saved/review/visit/user_profile) 삭제를 잇는 절차 — 최소한 수동 운영 절차라도 문서화, 가능하면 settings에 탈퇴 버튼 + Server Action.
  - [ ] 카카오/네이버 앱을 검수(운영) 모드로 전환 — 미검수 상태면 팀원 외 로그인 불가. **리드타임 있음, 제출 2주 전 착수.**
- **DoD**: 방침/약관 URL 존재, 소셜 3종을 임의 계정으로 로그인 가능(검수 통과).

---

## 8. P1 — 테스트·QA (Phase 5)

### 8.1 E2E 핵심 플로우 (Playwright)
- **할 일**: 아래 5개 시나리오만 얇게 (CI에서는 DB+mock 외부 API, 또는 로컬 전용으로 시작):
  - [ ] 홈 → 자연어 입력 → `/result` → 테마 → 코스 화면(타임라인 렌더).
  - [ ] 스팟 상세: 혼잡/날씨/적합성 배지 렌더(값 유무만).
  - [ ] 비로그인으로 저장 시도 → `/login` 리다이렉트.
  - [ ] (로그인 상태 픽스처) 저장 토글 → saved 화면 반영.
  - [ ] 리뷰 작성 → reviews 화면 반영.
- **DoD**: `pnpm --filter @eumgil/web test:e2e` green + CI 편입 여부 결정 기록.

### 8.2 회귀 체크리스트 (수동, 배포 전 10분)
- **할 일**:
  - [ ] 17개 라우트 순회 스모크 체크리스트를 이 문서 부록 B로 작성(라우트·기대 화면·데이터 출처 한 줄씩).
- **DoD**: 배포 전 체크리스트 1회 수행 기록이 남는 습관 정착.

### 8.3 기존 유닛 테스트 CI 편입
- **문제**: 68개 테스트가 로컬 수동 실행에 의존. 배포 워크플로(빌드→배포)에 테스트 게이트가 없다.
- **할 일**:
  - [ ] `.github/workflows`에 PR/push 시 `pnpm typecheck` + `pnpm --filter @eumgil/web test` 잡 추가, deploy가 이를 needs로 의존.
- **DoD**: 테스트 실패 시 배포가 중단된다.

---

## 9. P2 — 개선 (여유 시)

- [ ] **날씨 캐시 키 최적화**: `wx:${spot.id}` 좌표별 → 같은 권역 스팟이 각각 KMA를 호출. 격자(nx,ny) 단위 키로 바꾸면 호출 수가 권역 수로 준다(§3.2 쿼터와 연동).
- [ ] **이미지 최적화**: `next/image` 미사용 시 TourAPI 원본 이미지가 그대로 로드됨 — 사용 현황 점검, `next.config.mjs`에 `images.remotePatterns`(tong.visitkorea.or.kr) 설정.
- [ ] **Sentry 등 에러 트래킹** (§5.3의 확장).
- [ ] **혼잡 모델 보정**: prior×계수 모델의 상수를 실 방문 데이터(visit 테이블) 축적 후 보정하는 절차 메모.
- [ ] **다중 인스턴스 대비**: 파일 캐시 → Redis/DB 백엔드(`CacheStore` 주입 구조라 교체 지점은 [server/cache.ts](../apps/web/src/server/cache.ts):173 한 곳).
- [ ] **saved 화면 "+" 버튼 등 동작 없는 UI 정리**(saved.tsx:32 아이콘 버튼 등 no-op 버튼 전수 점검).

---

## 10. 문서 정합성 (즉시)

- [x] **CLAUDE.md 전면 재구성** ✅ 2026-07-10 — 히스토리성 진행 기록을 모두 제거하고 불변 규칙·아키텍처(Keycloak 인증·에이전트 레이어·캐시·Server Actions 현행 반영)만 남김. 진행상태·블로커·다음 작업의 정본은 이 문서로 위임(문서 유지 규칙도 CLAUDE.md 컨벤션 섹션에 명시).
- [ ] [docs/구현-계획.md](구현-계획.md) Phase 4·5 체크리스트를 실제 완료 상태로 동기화.
- [x] 이 문서를 CLAUDE.md에서 링크 ✅ 2026-07-10 — "진행 상태 / 남은 작업 (정본 위치)" 섹션.

---

## 부록 A — 운영 필수 환경변수 (배포 환경 주입 목록)

| 키 | 필수 | 비고 |
|---|---|---|
| `NEXT_PUBLIC_APP_URL` | ✅ | 운영 도메인 |
| `AUTH_SECRET` | ✅ | 난수 32B+. §1.1 후 미설정 시 기동 실패 |
| `AUTH_URL` | ✅ | 운영 도메인 고정 (trustHost 의존 제거) |
| `AUTH_KEYCLOAK_ISSUER` / `_ID` / `_SECRET` | ✅ | 운영 Keycloak realm |
| `DATABASE_URL` (+`DATABASE_SCHEMA`) | ✅ | PostgreSQL |
| `TOUR_API_SERVICE_KEY` / `KMA_SERVICE_KEY` / `AIRKOREA_SERVICE_KEY` | ✅ | Decoding 원본 키 (이중 인코딩 주의 — http.ts 헤더 참고) |
| `EUMGIL_CACHE_DIR` | ✅ | 볼륨 마운트 경로 (§5.1) |
| `EUMGIL_AGENT_LLM` + `OPENAI_API_KEY`(/`_MODEL`/`_BASE_URL`) | 선택 | LLM 매칭 활성화 시 |
| `ROUTING_PROVIDER` + `KAKAO_MOBILITY_API_KEY` | 선택 | 실 이동시간 (§3.3) |
| `EUMGIL_DEBUG` | 선택 | 운영은 기본 미설정(§5.3에서 warn 정책 별도) |

## 부록 B — 배포 전 스모크 체크리스트

(§8.2에서 작성 예정 — 17개 라우트 × 기대 화면 1줄)
