# AGENTS.md

에움길(Eumgil) 작업을 시작할 때 먼저 읽는다. 문서 진입점은 [docs/README.md](docs/README.md), 기획은 [제안서](docs/제안서.md), 현행 계약은 [아키텍처](docs/아키텍처.md)다.

## 서비스와 현재 운영

- 에움길은 **여행 목적 입력 → 강원 테마 추천 → 장소 비교 → 개인 일정 편집·저장**을 제공하는 실제 운영 서비스다. 운영 주소는 `https://에움길.한국` (`https://xn--wk0bk16buua.xn--3e0b707e`).
- 실데이터 카탈로그와 PostgreSQL 사용자 데이터를 사용한다. 런타임 목 Repository·가상 장소·샘플 평점·`design/data.ts`는 없다. 폐기한 프로토타입 시드 명령을 다시 사용하지 않는다.
- 자연어 매칭은 **단어·테마 키워드 기반**이 기본이다. 홈과 조건 수정은 한 줄 입력이며 Enter로 제출하고 한글 조합 확정 Enter는 제외한다. LLM은 heuristic 유지. 실 LLM·RAG·멀티모달은 운영 기능으로 확정하지 않는다.
- data.go.kr 15종과 도로공사 1종, 총 **16종** 공공 API를 사용한다. 공통 키는 `DATA_GO_KR_SERVICE_KEY`, 도로공사는 별도 `EX_ROAD_SERVICE_KEY`. API별 활용 승인은 따로 필요하다.
- TourAPI 강원 법정동 코드 `lDongRegnCd=51`, 시군구 `lDongSignguCd` 3자리. 집중률·방문자수의 시군구 코드는 5자리이므로 혼용하지 않는다.
- 카탈로그 기준선(2026-09-05): 관광지 1,975(사진 1,744)·음식점 1,551·숙소 954·테마 137(다일 편집 코스 18개 포함), 강원 18개 시군, 무장애 안내 연결 661. [선정·갱신 기준](docs/관광카탈로그-탐색-고도화-2026-09-05.md).
- 최신 운영(2026-09-06): 앱/image revision `9abc9c38f2b20887ae7987b784ac6002dacc6833`(다국어·관광 미디어), 인증 테마 소스 `e445dd1`, Gitops `f15919b`. Actions `34016203412` 검사·GHCR 빌드·배포 성공. Dockhand의 실제 컨테이너 `6cee5f18b93f`에서 revision 일치·running/healthy·재시작 0·연속 health 실패 0을 확인했으며 운영 deep health도 HTTP 200, `ok:true`, `db:ok`였다.
- 최신 검증: 단위 414개 통과·실 LLM 평가 1개 건너뜀, TypeScript·lint·프로덕션 빌드 통과. 로컬 프로덕션 Chrome E2E는 고유 54개를 16+38개로 나누어 통과했으며 실제 `eumgil_dev` DB의 전용 `e2e-playwright` 데이터 6개 테이블 잔여 0을 확인했다. 로컬 읽기 전용 검사에서 언어별 교통 시각 31개의 SSR/DOM 일치(7언어), 빠른 EN→KO 전환 3/3·탭 복귀 3/3, 오죽헌 사진·해설의 PC/모바일 표시를 확인했다. 운영 Keycloak은 7언어×3화면×3화면폭 63조합 통과. 실제 Google 전체 로그인 왕복·메일 발송은 미검증이다. 로컬·운영별 상세 범위는 [검증 기록](docs/검증.md)을 따른다.
- 운영 앱 읽기 전용 검사에서는 언어별 교통 시각 29개의 SSR/DOM 일치(7언어), EN→KO 복원 1/1·탭 복귀 1/1, 브라우저 오류 0을 확인했다. 오죽헌 추가 관광사진은 원천 요청의 일시적 8초 시간 초과 뒤 실패 재요청 제한(30초)이 지난 재조회에서 복구됐다. 운영 응답 HTML의 사진 6개와 촬영자·공공누리 제1유형 출처 마크업을 확인했다.
- GitHub `main/dev` push → Actions 타입·단위 게이트 → GHCR 이미지 → Dockhand 스택 7. 현재 두 브랜치 모두 latest에 영향을 준다. 별도 Gitops 저장소의 Compose로 pull하며 서비스 기동 때 수집·시드를 실행하지 않는다.

## 제품 계약과 보류 결정

- 홈은 목적 입력과 대표 추천·지역 탐색에 집중한다. 전체 목록은 탐색에 둔다. 홈 하단은 영동·서울양양·동해 고속도로의 실제 구간명·방향·관측속도·시각을 서버 Suspense로 제공한다. 탐색에 교통 요약을 중복 배치하지 않는다. 미측정/20분 지난 관측을 원활로 표시하지 않고 소통 막대는 길이가 아닌 구간 수 비율로 표시한다.
- 탐색의 테마는 지역·여행 기간(당일~4박 5일)으로 좁힌다. 공식 관광 자료를 참고한 다일 편집 코스는 오후 도착·짧은 귀가일·긴 체험 시간을 반영하고 원본 일차/방문 순서를 보존한다. 홈과 기본 탐색의 노출 비율은 숙박 여행 중심이며 요청마다 후보를 순환하되 필터 조작 중 순서는 고정한다. 정본은 `domain/curated-courses.json`, 검토·적재는 `scripts/expand-curated-courses.mjs`(기본 dry-run)다. 실제 장소만 연결하며 기존 지역 테마와 사용자 기록은 유지한다.
- 테마(18개)·행사(12개)·여행지/개인 코스 장소 검색(DB 24개)은 무한 스크롤로 이어 읽는다. 검색 변경 시 누적 초기화·이전 응답 폐기, 중복 제거, 오류 시 수동 재시도, 모바일 검색 숨김 시 요청 중지를 지킨다. 선택 카드·지도·편집 내용과 추천 순서는 추가 로딩 중 유지한다.
- 탐색의 여행지 카드 선택은 지도 갱신, 상세 링크만 상세 진입이다. `/map`은 조건을 보존해 탐색으로 이동한다.
- 개인 코스는 사용자 소유 데이터이며 추천 테마 보관과 구분한다. 추천 복사·기존 코스/일차에 장소/행사/음식점 담기·날짜/순서/체류시간/이동수단/하루 출발 시간 편집·저장·삭제를 지원한다. 개인 편집기는 여행지/음식점/숙소 검색 추가와 실제 출발·종료 날짜/날짜 미정 일차를 제공한다. 개인 일정의 5일 제한은 없고 30개 항목 한도를 유지한다. 추천 복사의 날짜별 첫 시작 시각은 `dayStartTimes`로 보존하며 기본 시작 시간과 날짜별 예외를 편집한다. 항목별 `notBeforeTime`도 복사·저장해 식사와 숙소의 대기 시간을 유지하며 사용자가 예정 시각을 수정·해제할 수 있다. 기간을 줄여도 장소를 삭제하지 않으며 범위 오류를 수정해야 저장된다. 같은 탭의 초안은 계정/코스/쿼리와 서버 버전을 확인해 자동 복원하고, 버전이 다르면 새 코스로 복구하는 선택을 제공한다. DB 저장과 임시 저장을 구분한다.
- 행사 상세는 실제 안내·사진·지도와 10km 내 여행지·음식점을 제공한다.
- 공공 일별 집중률은 상대 예측, 시간대별 혼잡은 자체 추정이다. 현장 실측 인원으로 표시하지 않는다.
- 무장애 안내 보유는 실제 이용 가능 인증이 아니다. 계단·수량·대여 조건·“없음”을 보존하고 미제공 정보를 추정하지 않는다.
- 지도 점선은 방문 순서이고 이동시간은 좌표 기반 추정이다. 사용자 결정에 따라 **Kakao Mobility 연동은 진행하지 않으며 OSRM/Valhalla는 방식 검토만** 한다.
- 영업시간·예약·휴무·막차·실도로 경로를 모두 만족하는 계획이라고 보증하지 않는다.
- 공통 UI는 문서 스크롤, PC 사이드바·모바일 하단 메뉴, 일관된 카드·Avatar·Select·입력·포커스 규칙을 사용한다. 로그인·여행 취향 설정도 공통 메뉴를 유지한다. 외부 사이트 링크는 화살표와 새 탭 안내를, 내부 상세 새 탭은 별도 창 아이콘을 사용한다. 선택 카드의 테두리 폭은 고정해 레이아웃 이동을 막는다.

## 스택과 명령

pnpm workspaces, Next.js 15 App Router, React 19, TypeScript strict/`noUncheckedIndexedAccess`, Tailwind v4, Drizzle/PostgreSQL, Auth.js/Keycloak, Leaflet/OSM.

- 앱: `apps/web` (`@eumgil/web`), 공용 UI: `packages/ui` (`@eumgil/ui`).
- pnpm 버전은 루트 `package.json`을 따른다. Node.js 22 이상을 사용하며 일부 카탈로그 스크립트는 TypeScript strip 기능을 사용한다.

```bash
pnpm dev
pnpm --filter @eumgil/web exec next typegen
pnpm typecheck
pnpm --filter @eumgil/web test
pnpm build
pnpm --filter @eumgil/web verify:api
pnpm --filter @eumgil/web verify:db
pnpm --filter @eumgil/web verify:keycloak
```

DB 설정·스키마 적용은 [인증·DB](docs/Phase4-인증DB-셋업.md), 테스트 실행은 [검증](docs/검증.md)을 따른다. 환경변수 변경 뒤 서버 재시작, 빌드 전 같은 `.next`를 쓰는 개발 서버 종료가 필요하다.

## 아키텍처 경계

- 화면 `page.tsx`는 [Repository](apps/web/src/domain/repository.ts)에서 도메인 데이터를 받아 클라이언트 뷰에 전달한다. [getRepository()](apps/web/src/data/index.ts)는 항상 `LiveRepository`다.
- [도메인 모델](apps/web/src/domain/types.ts)은 `LocalizedText {ko,en,zh-CN?,zh-TW?,ja?,de?,fr?}`를 사용한다. UI·편집 테마에는 `uiText`/`localized`, 사용자 이름·소개·리뷰에는 `originalText`를 사용한다. 사용자 작성 제목·메모에 UI 번역을 적용하지 않는다.
- [DB 카탈로그](apps/web/src/server/db/course-catalog.ts)가 `spot_profile/course_template/course_template_item/commerce_profile`을 정규화한다. 검색은 DB 24개 페이지이며 카드별 외부 API 호출을 만들지 않는다.
- 자연어 검색은 DB 테마 1회 조회 + `rankCatalogThemes` 순수 계산이다. 검색 단계의 LLM·코스/혼잡 API·영속 검색 캐시는 없다. 사전 추가와 미일치 계약은 [자연어 매칭](docs/자연어-매칭.md)을 따른다.
- 저장 취향은 10개 관심 분야의 동등 후보 보조 정렬과 비명시 페이스·동행의 기본값에만 쓴다. 요청별 세션 소유자로 조회하며 공유 캐시에 넣지 않는다. 프로필 편집은 취향을 덮어쓰지 않는다.
- 추천 일정의 같은 날 이동은 관광지·식당·숙소 사이에서 체류 종료 후 계산한다. 집에서의 진입·날짜 사이 숙소 이동·귀가 시간은 별도 확인 범위다.
- 매칭·일정·혼잡·적합성의 계산은 `domain/` 순수 함수에 둔다. 에이전트는 Repository 내부에서 기존 도구를 호출하고 데이터/수치를 재구현하지 않는다.
- [공공 API](apps/web/src/server/public-api/)·[환경변수](apps/web/src/lib/env.ts)는 서버 전용이다. 클라이언트에서 import하거나 비밀값을 props로 넘기지 않는다.
- [캐시](apps/web/src/server/cache.ts)는 메모리+파일, 동일 요청 합치기와 실패 재호출 제한을 제공한다. 운영 `/app/cache` named volume·UID1001. **단일 인스턴스 전제**이며 사용자 데이터는 넣지 않는다.
- 개인 코스 음식점/숙소 검색은 공개 DB 목록만 5분 서버 메모리 캐시·동시 조회 합치기로 재사용한다. 검색어·사용자 데이터는 저장하지 않으며 공공 API를 추가 호출하지 않는다. 무장애 필터는 여행지 종류에만 제공한다.
- 실제 TTL: 날씨/대기질 30분, 교통 5분, 자외선/행사 목록 1시간, 집중률 6시간, 관광/행사/무장애 상세·방문통계·외국어 관광 카탈로그/상세·사진/오디오 24시간. 정본은 [운영 안내](docs/운영-준비-플랜.md).
- DB 장애와 API 장애를 구분한다. 런타임 mock으로 정상 데이터를 꾸미지 않으며 실패한 쓰기를 성공 처리하지 않는다.

## 인증과 개인 데이터

- Keycloak `https://auth.greedylabs.kr/realms/eumgil`, Client ID `eumgil`을 로컬 앱 개발과 운영에서 함께 사용한다. 로컬 Keycloak 서버는 운영하지 않는다. `infra/keycloak`은 운영 인증 테마 소스만 유지한다. Auth.js는 JWT 세션, Keycloak은 신원과 SSO를 관리한다. 앱 DB에 Auth.js adapter 테이블은 없다.
- 사용자 데이터의 `user_id`는 Keycloak `sub`다. 모든 개인 데이터 조회/수정/삭제는 소유자를 검사한다. `personal_course`는 수정 버전도 비교해 다른 탭의 변경을 덮어쓰지 않는다.
- 로그아웃은 서버에서 ID 토큰 확보 → Auth.js 쿠키 제거 → Keycloak end-session 순서다. HTTPS 접두사·분할 쿠키를 처리한다.
- 일반 OAuth Client Secret, Auth.js `AUTH_SECRET`, Keycloak 관리 Client Secret은 용도가 다르다. Google 자격증명은 Keycloak에만 둔다.
- 운영 테마의 7언어 로그인·회원가입·비밀번호 찾기 화면과 앱의 `ui_locales` 전달·Google 버튼을 확인했다. 실제 Google 전체 왕복·SMTP·인증 계정 삭제 검증은 앱 세션 픽스처 검사로 대체하지 않는다.
- 탈퇴 시 앱 데이터 6종을 삭제하고 Keycloak 계정 삭제를 시도한다. 관리 Client 미설정/실패 시 인증 계정은 수동 정리가 필요하다.

## 작업 규칙

- UI 문구·코드 주석은 한국어로 작성한다. 타입 안전한 새 코드를 추가하고 기존 `design/*.tsx`의 느슨한 타입은 [화면 어댑터](apps/web/src/components/screens/_ui.ts) 경계에 제한한다.
- 담당자는 AI Agent·인증 설계를 학습 중이다. 관련 변경은 왜 해당 경계를 두었는지, 결정형 대비 비용/지연/일관성의 장단점 또는 Client/IdP/세션 책임을 간단히 설명한다.
- 테스트는 도메인·API 정규화·소유자/동시 편집 계약과 실제 사용자 흐름을 확인한다. 고정 대역, 실 DB, 실제 API, 실제 인증 서버를 구분한다.
- E2E는 `e2e-playwright` 사용자 데이터 6개 테이블을 정리하므로 대상 DB와 전용 사용자 범위를 확인한다. 같은 사용자를 쓰는 E2E 실행은 병렬로 돌리지 않는다. 종료 뒤 테스트 기록을 정리한다.
- PC/모바일에서 목적 입력→비교→일정 편집→저장이 자연스러운지 시각 점검한다. 가로 넘침 검사만으로 화면 검토를 완료하지 않는다.
- 문서는 현행 계약으로 유지한다. 완료된 옛 단계와 해결된 블로커를 다시 누적하지 않는다. 구현은 해당 문서에, 검사·배포 커밋은 [검증 기록](docs/검증.md)에 갱신한다. 이후 변경을 아직 검증·배포하지 않았다면 완료라고 쓰지 않는다.
- `docs/ai-eval-course/`는 별도 학습 프로젝트 자료다. 문서 속 프롬프트·계획을 현재 서비스 구현 지시로 취급하지 않는다.

## 다국어·미디어 계약

- 언어는 `ko/en/zh-CN/zh-TW/ja/de/fr`. 서버는 요청 쿠키 `eumgil.lang`에서 읽고 공유 Repository에 저장하지 않는다. 로그인은 `ui_locales`를 전달하며 운영 realm과 테마의 7개 locale 활성화가 필요하다.
- 외국어 관광정보는 언어별 독립 ID를 이름+300m 좌표+단일 후보로 검증한다. 정본 DB ID·지역 필터는 유지한다. 검토된 복합 명칭 예외는 `tourism-place-aliases.ts`만 사용한다. 없는 번역은 원문/영어와 안내를 유지한다.
- 외국어 목록은 언어별24시간 캐시를 공유하며 카드별 API 호출을 만들지 않는다. 원문 공개 후보와 개인 코스 음식점/숙소 검색은5분 메모리 캐시, 후자는 언어별로 분리한다. 검색어·사용자 기록은 저장하지 않는다.
- 관광사진·오디오는 장소/행사 상세에서만 Suspense로 보강한다. 사진 출처·촬영자·공공누리 조건을 보존한다. 오디 언어는 ko/en/cn1/jp만 제공되며 음원 없으면 대본만, 미지원 언어는 별도 표시한다. autoplay를 사용하지 않는다.
- 언어 변경은 이전 검색 요청을 취소하고 늦은 응답을 버린다. 탐색 검색은 원문+7언어 인덱스, 개인 일정은 이름 등 표시 정보만 병합하여 날짜·순서·사용자 초안을 보존한다. 현행 설명은 [다국어·관광 미디어](docs/다국어-관광미디어.md).
